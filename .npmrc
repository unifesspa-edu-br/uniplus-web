# Faz npm respeitar o campo engines.node do package.json — instalação falha
# em versões fora do range em vez de apenas avisar (EBADENGINE warning → erro).
# Combinado com .nvmrc, garante que o setup local não diverge silenciosamente
# da versão alvo do projeto.
engine-strict=true
